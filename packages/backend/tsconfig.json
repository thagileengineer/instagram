{
  // A configuration designed for Node.js using Vite for bundling.
  "compilerOptions": {
    /* Project Options */
    "target": "ES2022",                       /* Specify ECMAScript target version. Vite prefers modern ES. */
    "module": "NodeNext",                     /* Use ES modules (import/export) compatible with Node.js. */
    "moduleResolution": "NodeNext",           /* Strategy for resolving modules, aligned with NodeNext. */
    "lib": ["ES2022"],                        /* Specify library files to be included in the compilation. */
    "rootDir": "./src",                       /* Specify the root directory of source files. */
    "outDir": "./dist",                       /* Specify the output directory for built code. */
    "esModuleInterop": true,                  /* Allow default imports from modules with no default export. */
    "forceConsistentCasingInFileNames": true, /* Ensure file names are cased correctly. */

    /* Type Checking */
    "strict": true,                           /* Enable all strict type-checking options. Highly recommended. */
    "skipLibCheck": true,                     /* Skip type checking of declaration files. */

    /* Source Maps */
    "sourceMap": true,                        /* Generates corresponding .map file. Useful for debugging. */

    /* Vite specific: Enables dynamic import support */
    "resolveJsonModule": true
  },
  "include": [
    "src/**/*" // Include all TypeScript files inside the src directory
  ],
  "exclude": [
    "node_modules",
    "dist" // Exclude the build output directory
  ]
}
